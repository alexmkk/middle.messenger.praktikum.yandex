<div class="{{styles.header}}">
    <div class="{{styles.photo}}"></div>
    <div class="{{styles.username}} text-bold">{{ activeChat }}</div>
    <div class="{{styles.moreInfo}}">
        <a href="#">...</a>
    </div>
</div>